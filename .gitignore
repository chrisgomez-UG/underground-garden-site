# Build output — regenerated by `python3 build.py` (Netlify runs this itself
# on every deploy; nothing here is hand-edited, see README's "Deploying" section)
dist/

# Superseded by git+Netlify continuous deploy — no longer used, safe to ignore
# if it still exists locally.
_netlify_deploy/

__pycache__/
*.pyc
.DS_Store
